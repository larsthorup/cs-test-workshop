using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using EmailLib;
using Rhino.Mocks;

namespace EmailLibTestMsRhino
{
    [TestClass]
    public class NotifierTest
    {
        //[TestMethod]
        //public void Trigger()
        //{
        //    var emailService = MockRepository.GenerateStub<IEmailService>();
        //    var notifier = new Notifier(emailService);

        //    notifier.Trigger("New highscore: 150");

        //    // emailService.AssertWasCalled(es => es.SendEmail(Arg<Email>.Matches(delegate(Email e) { return e.Body == "New highscore: 150"; })));
        //}
    }
}
